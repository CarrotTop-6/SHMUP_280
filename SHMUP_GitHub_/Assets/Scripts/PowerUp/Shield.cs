using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Jack Bradford
//Tried to see if I could change the power up data inside the script, but found it easier to do through PlayerController
//11/3/232
public class Shield : BasePowerUp
{
    // Start is called before the first frame update
    void Start()
    {
        timer = 5;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
